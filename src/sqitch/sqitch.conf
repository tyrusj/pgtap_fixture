[core]
	engine = pg
	plan_file = upgrade.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[deploy]
	verify = true
[rebase]
	verify = true
[add]
	all = true
	template_directory = templates
[tag]
	all = false
[target "upgrade17"]
	uri = db:pg://nonadmin:password@localhost:5432/upgrade_debug
	plan_file = upgrade.plan
[target "install17"]
	uri = db:pg://nonadmin:password@localhost:5432/install_debug
	plan_file = install.plan